/* Preview Overlay */
function miss_preview_overlay(el) {
/*
	jQuery(el).hover(function(){
		if (jQuery(this).find('.data-image').length > 0) {
			var $img = jQuery(this).find('.data-image'),
				width = $img.width(),
				height = $img.height(),
				title = $img.attr('title');
		} else {
			var $img = jQuery(this).find('img'),
				width = $img.width(),
				height = $img.height(),
				title = $img.attr('title');
		}
		if (width) {
			jQuery(this).find('img').stop().animate({opacity : '0.8'}, 300);
			jQuery(this).append('<div class="preview" style="width:'+width+'px; height:'+height+'px"><div class="inner"><div class="icon"></div><h4>' + title + '</h4></div></div>');
			jQuery(this).find('.preview').animate({opacity : '1'}, 300);
		}

		}, function(){
		var $img = jQuery(this).find('img');
		jQuery(this).find('img').stop().animate({opacity : '1'}, 300);
		jQuery(this).find('.preview').animate({opacity : '0'}, 300 ,function(){ 
			jQuery(this).remove(); 
		});
		jQuery(this).find('.preview').css({left: '-230px;'});
	});
*/
}
